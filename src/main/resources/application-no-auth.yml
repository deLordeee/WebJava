# Профіль без аутентифікації для локальної розробки
spring:
  config:
    activate:
      on-profile: no-auth

app:
  security:
    enabled: false

logging:
  level:
    org.springframework.security: WARN